# План тестування системи на стійкість

## 1. Мета тестування

Перевірити систему на стійкість до відмов, вразливостей та некоректних даних, виявлених в аналізі системи.

## 2. Типи тестів

### 2.1 Функціональні тести
- Перевірка основного функціоналу
- Позитивні і негативні сценарії
- Крайові випадки (edge cases)

### 2.2 Тести на стійкість (Resilience Tests)
- Обробка помилок бази даних
- Обробка мережевих помилок
- Відновлення після збоїв

### 2.3 Тести безпеки (Security Tests)
- SQL Injection
- XSS атаки
- CSRF атаки
- Brute-force атаки
- JWT токени

### 2.4 Навантажувальні тести (Load Tests)
- Одночасні запити
- Rate limiting
- Продуктивність під навантаженням

## 3. Тестові сценарії

### 3.1 Backend тести

#### TEST-001: Реєстрація з дублікатами (Race Condition)
**Категорія**: Resilience  
**Пріоритет**: 🔴 Критичний  
**Мета**: Перевірити обробку одночасної реєстрації користувачів з однаковим username

**Кроки:**
1. Відправити 2 паралельних POST запити на `/register` з однаковим `user_name`
2. Перевірити відповіді обох запитів
3. Перевірити стан бази даних

**Очікуваний результат:**
- Один запит отримує 201 Created + токен
- Другий запит отримує 400 Bad Request "User already exists"
- В базі даних тільки 1 користувач з цим username

**Фактичний результат:** _заповнюється при тестуванні_

---

#### TEST-002: Створення замовлення з некоректними даними
**Категорія**: Functional  
**Пріоритет**: 🔴 Критичний  
**Мета**: Перевірити валідацію даних замовлення

**Кроки:**
1. Відправити POST `/orders` з `quantity: -5`
2. Відправити POST `/orders` з `price: -100`
3. Відправити POST `/orders` з порожнім масивом `items: []`
4. Відправити POST `/orders` з `quantity: 0`

**Очікуваний результат:**
- Всі запити повертають 422 Validation Error
- Замовлення не створюються в базі даних
- Повертаються зрозумілі повідомлення про помилки

**Фактичний результат:** _заповнюється при тестуванні_

---

#### TEST-003: Транзакційність створення замовлення
**Категорія**: Resilience  
**Пріоритет**: 🔴 Критичний  
**Мета**: Перевірити, що при помилці створення OrderItem відкочується весь Order

**Кроки:**
1. Модифікувати код для симуляції помилки при створенні 2-го OrderItem
2. Створити замовлення з 3 товарами
3. Перевірити стан бази даних

**Очікуваний результат:**
- Запит повертає 500 Internal Server Error
- В базі даних немає Order
- В базі даних немає жодного OrderItem

**Фактичний результат:** _заповнюється при тестуванні_

---

#### TEST-004: SQL Injection в полях
**Категорія**: Security  
**Пріоритет**: 🔴 Критичний  
**Мета**: Перевірити захист від SQL injection

**Кроки:**
1. POST `/register` з `user_name: "admin' OR '1'='1"`
2. POST `/auth` з `user_name: "admin'--"`
3. POST `/orders` з `name: "'; DROP TABLE orders; --"`

**Очікуваний результат:**
- Всі запити обробляються безпечно (ORM екранує спецсимволи)
- Таблиці не видаляються
- Дані зберігаються як текст без виконання

**Фактичний результат:** _заповнюється при тестуванні_

---

#### TEST-005: JWT токен з компрометованим SECRET_KEY
**Категорія**: Security  
**Пріоритет**: 🔴 Критичний  
**Мета**: Перевірити можливість підробки токенів

**Кроки:**
1. Згенерувати JWT токен з `{"sub": "admin", "role": "admin"}` використовуючи SECRET_KEY
2. Відправити GET `/orders/my` з цим токеном
3. Перевірити доступ

**Очікуваний результат:**
- Якщо SECRET_KEY = "SECRET_KEY_CHANGE_ME" → доступ дозволено (ВРАЗЛИВІСТЬ!)
- Якщо SECRET_KEY з .env → доступ заборонено

**Фактичний результат:** _заповнюється при тестуванні_

---

#### TEST-006: Brute-force атака на /auth
**Категорія**: Security  
**Пріоритет**: 🔴 Критичний  
**Мета**: Перевірити захист від підбору паролів

**Кроки:**
1. Створити користувача `testuser`
2. Відправити 100 POST `/auth` з різними паролями
3. Перевірити час відповіді та статус коди

**Очікуваний результат:**
- Після 5-10 невдалих спроб - тимчасове блокування (429 Too Many Requests)
- Або значне уповільнення відповідей
- Логування спроб

**Фактичний результат:** _заповнюється при тестуванні_

---

#### TEST-007: Rate limiting на /register
**Категорія**: Security / Load  
**Пріоритет**: 🔴 Критичний  
**Мета**: Перевірити захист від спам-реєстрацій

**Кроки:**
1. Відправити 50 POST `/register` за 10 секунд
2. Перевірити відповіді

**Очікуваний результат:**
- Після 10-20 запитів повертається 429 Too Many Requests
- Або використовується CAPTCHA

**Фактичний результат:** _заповнюється при тестуванні_

---

#### TEST-008: Валідація сили пароля
**Категорія**: Security  
**Пріоритет**: 🟠 Високий  
**Мета**: Перевірити вимоги до складності пароля

**Кроки:**
1. POST `/register` з паролем "123"
2. POST `/register` з паролем "p" (1 символ)
3. POST `/register` з паролем "password" (без цифр/спецсимволів)

**Очікуваний результат:**
- Всі запити повертають 422 Validation Error
- Повідомлення: "Password must be at least 8 characters, contain uppercase, lowercase, digit"

**Фактичний результат:** _заповнюється при тестуванні_

---

#### TEST-009: CORS налаштування
**Категорія**: Security  
**Пріоритет**: 🔴 Критичний  
**Мета**: Перевірити обмеження CORS

**Кроки:**
1. Відправити запит з Origin: `http://malicious-site.com`
2. Перевірити CORS заголовки

**Очікуваний результат:**
- Access-Control-Allow-Origin НЕ містить "*"
- Access-Control-Allow-Origin містить тільки дозволені домени

**Фактичний результат:** _заповнюється при тестуванні_

---

#### TEST-010: Навантажувальний тест /products
**Категорія**: Load  
**Пріоритет**: 🟡 Середній  
**Мета**: Перевірити продуктивність отримання списку продуктів

**Кроки:**
1. Додати 1000 продуктів в базу даних
2. Відправити 100 одночасних GET `/products`
3. Виміряти час відповіді

**Очікуваний результат:**
- 95% запитів обробляються за < 1 секунду
- Немає тайм-аутів
- Немає помилок 500

**Фактичний результат:** _заповнюється при тестуванні_

---

### 3.2 Frontend тести

#### TEST-011: Обробка помилки завантаження продуктів
**Категорія**: Resilience  
**Пріоритет**: 🟡 Середній  
**Мета**: Перевірити UX при недоступності backend

**Кроки:**
1. Вимкнути backend сервер
2. Відкрити MainPage
3. Перевірити відображення помилки

**Очікуваний результат:**
- Відображається зрозуміле повідомлення про помилку
- Є кнопка "Спробувати ще раз"
- Немає білого екрану або console errors

**Фактичний результат:** _заповнюється при тестуванні_

---

#### TEST-012: XSS через product name
**Категорія**: Security  
**Пріоритет**: 🔴 Критичний  
**Мета**: Перевірити санітизацію HTML в назвах продуктів

**Кроки:**
1. Через API створити продукт з назвою `<script>alert('XSS')</script>`
2. Відкрити MainPage
3. Перевірити виконання скрипту

**Очікуваний результат:**
- Скрипт НЕ виконується
- Назва відображається як текст: `<script>alert('XSS')</script>`
- React автоматично екранує HTML

**Фактичний результат:** _заповнюється при тестуванні_

---

#### TEST-013: Валідація форми замовлення
**Категорія**: Functional  
**Пріоритет**: 🟡 Середній  
**Мета**: Перевірити валідацію на frontend

**Кроки:**
1. Відкрити кошик
2. Натиснути "Оформити замовлення"
3. Спробувати відправити форму з порожніми полями
4. Ввести некоректний email "test@"
5. Ввести некоректний телефон "123"

**Очікуваний результат:**
- Форма не відправляється
- Відображаються повідомлення про помилки валідації
- Підсвічуються поля з помилками

**Фактичний результат:** _заповнюється при тестуванні_

---

#### TEST-014: Крадіжка токенів через XSS
**Категорія**: Security  
**Пріоритет**: 🟠 Високий  
**Мета**: Перевірити можливість доступу до localStorage через XSS

**Кроки:**
1. Ввести в console: `console.log(localStorage.getItem('token'))`
2. Перевірити доступність токена

**Очікуваний результат:**
- Токен доступний (ВРАЗЛИВІСТЬ - localStorage не захищений від XSS)
- Рекомендація: використовувати httpOnly cookies

**Фактичний результат:** _заповнюється при тестуванні_

---

#### TEST-015: Обробка timeout при створенні замовлення
**Категорія**: Resilience  
**Пріоритет**: 🟡 Середній  
**Мета**: Перевірити UX при тайм-ауті мережі

**Кроки:**
1. Налаштувати Chrome DevTools - Network throttling на "Offline"
2. Заповнити форму замовлення
3. Натиснути "Підтвердити замовлення"

**Очікуваний результат:**
- Відображається loader/spinner
- Після timeout показується повідомлення про помилку
- Користувач може повторити спробу
- Дані форми не втрачаються

**Фактичний результат:** _заповнюється при тестуванні_

---

## 4. Інструменти тестування

### 4.1 Backend тестування
- **pytest** - фреймворк для тестування Python
- **pytest-asyncio** - для асинхронних тестів FastAPI
- **httpx** - HTTP клієнт для тестування API
- **locust** - для навантажувального тестування
- **sqlalchemy-utils** - для роботи з тестовою БД

### 4.2 Frontend тестування
- **Jest** - unit тести для React
- **React Testing Library** - тести компонентів
- **Playwright** - E2E тести
- **Chrome DevTools** - manual тестування мережі

### 4.3 Security тестування
- **OWASP ZAP** - автоматичне сканування вразливостей
- **sqlmap** - тестування SQL injection
- **Burp Suite** - перехоплення та модифікація запитів

## 5. Структура тестів

```
Python-web/
├── server/
│   ├── tests/
│   │   ├── test_auth.py          # TEST-001, TEST-005, TEST-006, TEST-008
│   │   ├── test_orders.py        # TEST-002, TEST-003
│   │   ├── test_security.py      # TEST-004, TEST-007, TEST-009
│   │   ├── test_products.py      # TEST-010
│   │   └── conftest.py           # Fixtures для тестів
│   └── ...
├── client/
│   ├── src/
│   │   ├── __tests__/
│   │   │   ├── MainPage.test.jsx # TEST-011, TEST-012, TEST-013
│   │   │   └── Auth.test.jsx     # TEST-014, TEST-015
│   │   └── ...
└── docs/
    ├── 01-system-analysis.md
    ├── 02-testing-plan.md
    └── 03-test-protocols.md     # Результати тестування
```

## 6. Команди для запуску тестів

### Backend
```bash
cd server

# Встановити залежності
pip install pytest pytest-asyncio httpx locust

# Запустити всі тести
pytest tests/ -v

# Запустити конкретний тест
pytest tests/test_auth.py::test_register_duplicate -v

# Запустити з coverage
pytest tests/ --cov=. --cov-report=html

# Навантажувальні тести
locust -f tests/load_test.py --host=http://localhost:8000
```

### Frontend
```bash
cd client

# Встановити залежності
npm install --save-dev @testing-library/react @testing-library/jest-dom jest

# Запустити тести
npm test

# E2E тести
npx playwright test
```

## 7. Критерії успішності

### 7.1 Функціональні тести
- ✅ 100% тестів проходять без помилок
- ✅ Всі edge cases обробляються коректно

### 7.2 Тести на стійкість
- ✅ Система відновлюється після помилок БД
- ✅ Транзакції відкочуються при помилках
- ✅ Мережеві помилки обробляються gracefully

### 7.3 Тести безпеки
- ✅ SQL injection НЕ працює (захищено ORM)
- ✅ XSS НЕ працює (React екранує HTML)
- ✅ Немає hardcoded secrets
- ✅ CORS налаштований правильно
- ✅ Rate limiting захищає від brute-force

### 7.4 Навантажувальні тести
- ✅ 100 одночасних користувачів без падіння
- ✅ 95% запитів < 1 секунда
- ✅ Немає memory leaks

## 8. Розклад тестування

| Етап | Тести | Відповідальний | Тривалість |
|------|-------|----------------|------------|
| 1. Підготовка | Налаштування інфраструктури | DevOps | 2 години |
| 2. Backend Security | TEST-004, TEST-005, TEST-006, TEST-007, TEST-009 | Backend Dev | 4 години |
| 3. Backend Resilience | TEST-001, TEST-002, TEST-003 | Backend Dev | 3 години |
| 4. Backend Load | TEST-010 | QA Engineer | 2 години |
| 5. Frontend Security | TEST-012, TEST-014 | Frontend Dev | 2 години |
| 6. Frontend Resilience | TEST-011, TEST-013, TEST-015 | Frontend Dev | 3 години |
| 7. Аналіз результатів | Всі | Team | 2 години |
| **ВСЬОГО** | | | **18 годин** |

## 9. Звітність

Результати кожного тесту документуються в [03-test-protocols.md](./03-test-protocols.md) з наступною інформацією:
- ID тесту
- Дата виконання
- Виконавець
- Фактичний результат
- Статус (✅ PASSED / ❌ FAILED / ⚠️ PARTIAL)
- Знімки екрану/логи (якщо є)
- Виявлені проблеми
- Рекомендації щодо виправлення

## 10. Пост-тестування

Після завершення тестування:
1. ✅ Створити GitHub Issues для виявлених проблем
2. ✅ Пріоритизувати виправлення за критичністю
3. ✅ Оновити документацію
4. ✅ Провести ретроспективу команди
5. ✅ Запланувати наступний цикл тестування
